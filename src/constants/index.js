/**
 * constants/index.js
 * Vishal Kumar
 */

`use strict`;

const db = require(`./db`);
const i18n = require(`./i18n`);
const server = require(`./server`);

module.exports = {
	db,
	i18n,
	server,
};
